import "../styles/global.css";

const app = ({ Component, pageProps }) => {
  return <Component {...pageProps} />;
};

export default app;

// This file offers a way to add to or customize all components(pages) being rendered
